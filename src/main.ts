// Einstiegspunkt der App. Die eigentliche UI folgt in den Meilensteinen M4–M7.
// In M1 ist dies nur ein Platzhalter, damit das Scaffold lauffähig baut.

const app = document.querySelector<HTMLDivElement>("#app");

if (app) {
  app.textContent = "Eisbachwelle — Gerüst steht. UI folgt.";
}
